apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-frontend
spec:
  template:
    spec:
      containers:
        - name: admin-frontend
          resources:
            requests:
              memory: "64Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"